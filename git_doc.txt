



git config --global user.email "mohdusman9474@gmail.com"
git config --global user.name "mohdusman74"

learn git paert -1:

1) git init
2) git add [filename] || -A
2) git status
3) git commit -m "msg"
5) git remote add origin repoName
6) git push origin master
7)git clone repoName
8)git pull origin master